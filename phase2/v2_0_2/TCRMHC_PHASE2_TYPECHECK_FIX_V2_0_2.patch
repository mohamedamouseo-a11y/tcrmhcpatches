diff --git a/apps/web/src/features/admin/DashboardPage.tsx b/apps/web/src/features/admin/DashboardPage.tsx
index b9c18706ab2089c5095bec08be74525c8b7b8588..eee8140e3c3e28932557f36c459edb10388e7ac4 100644
--- a/apps/web/src/features/admin/DashboardPage.tsx
+++ b/apps/web/src/features/admin/DashboardPage.tsx
@@ -7,6 +7,6 @@ export function DashboardPage({ lang }: { lang: Language }) {
   const [data, setData] = useState<Overview | null>(null); const [error, setError] = useState("");
   useEffect(() => { adminApi.overview().then(setData).catch(() => setError(lang === "ar" ? "تعذر تحميل لوحة التحكم" : "Could not load dashboard")); }, [lang]);
   if (error) return <ErrorNotice message={error} />; if (!data) return <Loading />;
-  const labels = lang === "ar" ? ["إجمالي العملاء","العملاء النشطون","إجمالي المستخدمين","المستخدمون النشطون","الجلسات النشطة","أحداث الأمان 24س"] : ["Total customers","Active customers","Total users","Active users","Active sessions","Security events 24h"];
+  const labels: readonly [string, string, string, string, string, string] = lang === "ar" ? ["إجمالي العملاء","العملاء النشطون","إجمالي المستخدمين","المستخدمون النشطون","الجلسات النشطة","أحداث الأمان 24س"] : ["Total customers","Active customers","Total users","Active users","Active sessions","Security events 24h"];
   return <><PageHeader title={lang === "ar" ? "لوحة تحكم المشرف العام" : "Super Admin Dashboard"} subtitle={lang === "ar" ? "نظرة عامة على منصة مركز المساعدة" : "Platform-wide Help Center overview"} /><section className="stats-grid"><StatCard label={labels[0]} value={data.totalTenants}/><StatCard label={labels[1]} value={data.activeTenants}/><StatCard label={labels[2]} value={data.totalUsers}/><StatCard label={labels[3]} value={data.activeUsers}/><StatCard label={labels[4]} value={data.activeSessions}/><StatCard label={labels[5]} value={data.securityEvents24h}/></section><section className="admin-panel"><h2>{lang === "ar" ? "آخر أحداث الأمان" : "Recent security events"}</h2><div className="compact-list">{data.recentSecurityEvents.map((e, i) => <div key={`${e.createdAt}-${i}`}><strong>{e.eventType}</strong><span>{e.outcome}</span><time>{new Date(e.createdAt).toLocaleString()}</time></div>)}</div></section></>;
 }
diff --git a/apps/web/src/features/admin/UserDetailPage.tsx b/apps/web/src/features/admin/UserDetailPage.tsx
index 03e976fed62db365d26ce4bee53d08e221ad0e7d..08cc81bf169e4e22f307f82b836e4e243ff682c1 100644
--- a/apps/web/src/features/admin/UserDetailPage.tsx
+++ b/apps/web/src/features/admin/UserDetailPage.tsx
@@ -8,6 +8,6 @@ export function UserDetailPage({lang,id}:{lang:Language;id:string}){
  const load=()=>Promise.all([adminApi.user(id),adminApi.tenants({pageSize:100}),adminApi.positions({pageSize:100}),adminApi.roles()]).then(([u,t,p,r])=>{setUser(u.user);setTenants(t.items);setPositions(p.items);setRoles(r.roles);setError("")}).catch(()=>setError(lang==="ar"?"تعذر تحميل المستخدم":"Could not load user"));useEffect(()=>{void load()},[id,lang]);
  const tenantMap=useMemo(()=>new Map(tenants.map(t=>[t.id,t.name])),[tenants]);if(error)return <ErrorNotice message={error}/>;if(!user)return <Loading/>;
  async function status(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);try{await adminApi.updateUser(id,{status:String(f.get("status"))});void load()}catch{setError(lang==="ar"?"تعذر تحديث الحالة":"Could not update status")}}
- async function addMembership(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const next:Membership[]=[...(user.memberships??[]).map(m=>({tenantId:m.tenantId,roleId:m.roleId,positionId:m.positionId??null,status:m.status})),{tenantId:String(f.get("tenantId")),roleId:String(f.get("roleId")),positionId:String(f.get("positionId"))||null,status:"active"}];try{await adminApi.updateUser(id,{memberships:next});e.currentTarget.reset();void load()}catch{setError(lang==="ar"?"تعذر تحديث العضوية":"Could not update membership")}}
+ async function addMembership(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const next:Membership[]=[...(user?.memberships??[]).map(m=>({tenantId:m.tenantId,roleId:m.roleId,positionId:m.positionId??null,status:m.status})),{tenantId:String(f.get("tenantId")),roleId:String(f.get("roleId")),positionId:String(f.get("positionId"))||null,status:"active"}];try{await adminApi.updateUser(id,{memberships:next});e.currentTarget.reset();void load()}catch{setError(lang==="ar"?"تعذر تحديث العضوية":"Could not update membership")}}
  return <><PageHeader title={user.username} subtitle={user.email}/><section className="detail-grid"><article className="admin-panel"><h2>{lang==="ar"?"حالة الحساب":"Account status"}</h2><form className="admin-form" onSubmit={status}><select name="status" defaultValue={user.status}><option>active</option><option>disabled</option><option>locked</option></select><button className="primary-small">{lang==="ar"?"حفظ":"Save"}</button></form></article><article className="admin-panel"><h2>{lang==="ar"?"إضافة عضوية":"Add membership"}</h2><form className="admin-form" onSubmit={addMembership}><select name="tenantId" required defaultValue=""><option value="" disabled>{lang==="ar"?"العميل":"Customer"}</option>{tenants.map(t=><option key={t.id} value={t.id}>{t.name}</option>)}</select><select name="roleId" required defaultValue=""><option value="" disabled>{lang==="ar"?"الدور":"Role"}</option>{roles.map(r=><option key={r.id} value={r.id}>{r.key}</option>)}</select><select name="positionId" defaultValue=""><option value="">{lang==="ar"?"بدون منصب":"No position"}</option>{positions.map(p=><option key={p.id} value={p.id}>{tenantMap.get(p.tenantId)} · {lang==="ar"?p.nameAr:p.nameEn}</option>)}</select><button className="primary-small">{lang==="ar"?"إضافة":"Add"}</button></form></article></section><section className="admin-panel table-wrap"><h2>{lang==="ar"?"عضويات العملاء":"Customer memberships"}</h2><table><thead><tr><th>{lang==="ar"?"العميل":"Customer"}</th><th>{lang==="ar"?"الدور":"Role"}</th><th>{lang==="ar"?"المنصب":"Position"}</th><th>Status</th></tr></thead><tbody>{(user.memberships??[]).map((m,i)=><tr key={m.id??i}><td>{m.tenantName}</td><td>{m.roleKey}</td><td>{lang==="ar"?(m.positionNameAr??"—"):(m.positionNameEn??"—")}</td><td><StatusBadge status={m.status}/></td></tr>)}</tbody></table></section></>;
 }
